apiVersion: v1
kind: Service
metadata:
  name: frontend-{{ .Values.author }}
  labels:
    app: frontend
    author: {{ .Values.author }}
spec:
  ports:
    - name: web
      port: 80
      targetPort: 8080
  selector:
    app: frontend
    author: {{ .Values.author }}